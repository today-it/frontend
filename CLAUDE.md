@AGENTS.md

# Claude 전용 메모

공통 작업 규칙의 원본은 `AGENTS.md`다.
이 파일에는 Claude 도구에만 해당하는 사항을 작성한다.
공통 규칙을 중복하거나 다른 내용으로 재정의하지 않는다.

- 레퍼런스 조사·비교 시안·사용자 선택·구현·전수 검증 순서를 따른다.
- 비교 시안은 사용 가능한 아티팩트 기능이나 로컬 파일로 제공한다.
  사용자가 다시 열어 비교할 수 있게 한다.
- 브라우저 도구를 사용할 수 있으면 실제 화면을 직접 조작해 검증한다.
  사용할 수 없으면 코드 검사만으로 화면 검증을 완료했다고 보고하지 않는다.
- 로그인 상태가 필요한 검수는 사용자가 연결한 브라우저 세션을 사용한다.
  인증을 우회하거나 인증 정보를 추출하지 않는다.
- 작업 중 직접 만든 임시 탭은 검수 후 정리한다.
  사용자가 열어 둔 탭은 임의로 닫지 않는다.