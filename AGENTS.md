<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# AGENTS.md

이 파일을 모든 에이전트의 작업 규칙 원본으로 사용한다.
디자인·레이아웃·기능 규칙은 별도 문서에서 관리하며 중복 작성하지 않는다.

## 판단 기준

1. 사용자의 최신 결정과 선택한 시안
2. 프로젝트의 디자인 가이드와 기능 규칙
3. 현재 코드·테스트·README

- 디자인 가이드: `docs/design/README.md`
- 기능 규칙: `docs/design/product-rules.md`
- 결정 기록: `docs/design/decisions.md`

위 경로는 프로젝트의 실제 문서 위치에 맞춘다.
문서가 없으면 있다고 가정하지 않는다.
문서와 코드가 충돌하면 차이를 확인하고 해결한다.
폐기된 문서나 과거 구현을 사용자 요청 없이 현재 요구사항으로 되살리지 않는다.

## 작업 원칙

- 답변은 한국어로 한다.
- 작업 전에 관련 지침과 `git status`를 확인한다.
- 커밋되지 않은 사용자 변경과 작업에 관계없는 파일을 건드리지 않는다.
- 저장소에서 확인할 수 있는 사실은 코드·테스트·브라우저로 직접 확인한다.
- 사용자가 결정한 요구사항을 임의로 축소하거나 다른 안으로 바꾸지 않는다.
- 요청 범위를 벗어나는 재설계·리팩터링은 먼저 제안한다.
- 이미 승인된 범위의 구현과 검증은 불필요한 재확인 없이 진행한다.
- 기존 기능과 데이터 의미를 의도치 않게 바꾸지 않는다.
- 작업 중 발견한 문제·제약·방향 변경을 간결하게 알린다.

## Git과 산출물

- Git 변경 작업은 사용자가 한다.
  스테이징·커밋·푸시·브랜치 생성·PR 생성은 직접 실행하지 않는다.
  상태·차이·이력 조회는 가능하다.
- 작업 종료 시 변경 파일을 구분하고 복사 가능한 `git add` 명령과
  커밋 제목을 제공한다.
- 커밋 제목은 영어 type + 한국어 설명으로 작성한다.
  예: `fix: 검색 결과의 모바일 레이아웃 수정`
- 관련 없는 변경을 포함하는 `git add .` 명령은 제공하지 않는다.
- 대용량 임시 데이터·분석 산출물·디버깅 파일을 소스 변경에 섞지 않는다.
- 다시 생성할 수 있는 대용량 산출물은 별도 보관하고 Git에서 제외한다.
  필요한 생성 코드·간결한 결과 요약·재현 방법은 유지한다.

## 디자인 조사와 시안 선택

### 적용 범위

- 새 페이지·주요 컴포넌트·사용자 흐름을 설계하거나 재설계할 때는
  구현 전에 레퍼런스 조사와 비교 시안을 제시한다.
- 이미 승인된 디자인의 단순 적용이나 오류 수정은 시안 선택을 반복하지 않는다.

### 레퍼런스 조사

- UI/UX가 우수한 실서비스와 관련 디자인 가이드 여러 곳을 조사한다.
- 유명한 서비스뿐 아니라 대상 사용자·기능·정보 구조가 유사한 사례를 포함한다.
- 조사 범위는 작업 규모에 맞추고, 사용자가 조사 개수를 지정하면 따른다.
- 화면 구성·정보 우선순위·탐색·입력·피드백·로딩·빈 상태·오류 처리를
  이번 작업과 관련된 범위에서 확인한다.
- 여러 독립적인 서비스와 가이드에서 반복되는 패턴을 우선 검토한다.
  유명세나 사용 빈도만으로 좋은 UX라고 판단하지 않는다.
- 레퍼런스 URL과 확인 내용을 남기고,
  직접 관찰한 사실·가이드의 명시적 규칙·에이전트의 해석을 구분한다.
- 확인하지 못한 동작은 추측하지 않고 ‘미확인’으로 표시한다.
- 레퍼런스를 결론에 끼워 맞추거나 사이트의 시각적 표현을 그대로 복제하지 않는다.
- 기존 프로젝트 규칙과 사용자 결정을 우선한다.
  외부 패턴을 도입하려면 적합성과 변경 이유를 설명한다.

### 비교 시안

- 기본적으로 A/B/C 3개 시안을 제시한다.
  의미 있는 대안이 2개라면 억지로 세 번째 안을 만들지 않는다.
- 색상만 바꾸는 안이 아니라 배치·정보 밀도·탐색·조작 방식 등에서
  비교할 가치가 있는 차이를 만든다.
- 같은 콘텐츠와 화면 크기로 비교할 수 있게 시각화한다.
- 동작 차이가 중요하면 인터랙티브 시안을 사용한다.
- 각 안의 참고 근거·장점·단점·적용 조건을 짧게 설명한다.
- 추천안 하나를 표시하고 추천 이유를 설명한다.
- 시안은 사용자가 다시 열어 볼 수 있는 파일이나 아티팩트로 남긴다.

### 선택과 구현

- 사용자가 선택하기 전에는 해당 디자인을 실제 제품에 적용하지 않는다.
  레퍼런스 조사·시안 제작·독립적인 기술 검증은 계속 진행할 수 있다.
- 선택된 시안을 구현 기준으로 삼는다.
  다른 안의 요소를 승인 없이 섞거나 임의로 절충하지 않는다.
- 새로운 디자인 토큰이나 주요 동작 규칙이 필요하면 시안 단계에서 제안한다.
- 승인된 방향 안의 세부 구현은 기존 규칙에 맞춰 진행한다.
- 기술적 제약으로 주요 배치나 동작을 바꿔야 하면
  차이와 대안을 보여 주고 다시 선택받는다.
- 확정된 공통 규칙은 디자인 가이드에 반영한다.

## 프론트엔드 구현

- 기존 디자인 토큰과 공용 컴포넌트를 우선 사용한다.
- 공통 규칙은 공용 소스에서 수정한다.
  페이지별 스타일로 공용 컴포넌트의 규격을 덮어쓰지 않는다.
- 정상 화면뿐 아니라 로딩·빈 데이터·오류·재시도·성공 상태를 함께 구현한다.
- 내부 구현 용어나 디버깅 정보를 사용자 화면에 불필요하게 노출하지 않는다.
- 접근성과 반응형 동작을 구현 과정부터 고려한다.
- 의존성 추가나 구조 변경 전 기존 기능으로 해결할 수 있는지 확인한다.

## Next.js 작업 기준

- 설치된 Next.js 버전과 사용하는 라우터 구조를 먼저 확인한다.
  다른 버전의 예제나 API를 그대로 적용하지 않는다.
- 기존 서버·클라이언트 경계를 존중한다.
  브라우저 기능이나 상호작용에 필요한 범위만 클라이언트로 둔다.
- 서버 전용 코드·비밀값·인증 정보가 클라이언트로 전달되지 않도록 한다.
- 캐시를 추가하거나 변경할 때 데이터의 공개 범위·최신성·무효화 시점을 확인한다.
  개인화 데이터가 사용자 간 공유되지 않도록 한다.
- 데이터 변경 후 관련 화면과 파생 데이터가 갱신되는지 확인한다.
  무조건적인 새로고침이나 캐시 비활성화로 원인을 덮지 않는다.
- 로딩·오류 경계는 실제 데이터 요청과 화면 전환 구조에 맞게 배치한다.
- 프로젝트에서 사용하는 패키지 매니저와 잠금 파일을 유지한다.

## 데이터와 실행 환경

- 개발 서버라는 이유만으로 테스트 데이터에 연결됐다고 가정하지 않는다.
- 데이터 변경이나 E2E 실행 전 대상 환경을 확인한다.
- 운영 DB에는 직접 쓰지 않는다.
  운영 데이터 변경이 필요하면 사용자가 검토하고 실행할 수 있도록
  사전 확인·변경·결과 확인 절차를 제공한다.
- 저장·제출·삭제를 포함한 동작 테스트는 격리된 테스트 환경에서 수행한다.
- 로그인은 사용자가 진행한다. 인증을 우회하지 않는다.
- 실제 계정으로 화면을 검수할 때 운영 데이터를 변경하지 않는다.
- 사용자가 실행 중인 개발 서버가 있으면 우선 사용한다.
  임의로 종료하거나 중복 서버를 실행하지 않는다.

## 구현 완료 후 전수 검증

구현만 끝났다고 작업 완료로 보고하지 않는다.
변경한 기능 전체와 영향을 받는 화면을 대상으로
자동 검사·실제 동작·디자인 가이드 준수 여부를 검증한다.

### 검증 범위

- 변경한 페이지·컴포넌트·사용자 흐름의 검증 항목을 정리한다.
- 정상 상태뿐 아니라 로딩·빈 데이터·오류·재시도·입력 오류를 포함한다.
- 관련된 경우 로그인·권한·데이터 유무에 따른 차이도 확인한다.
- 공용 컴포넌트 변경 시 사용하는 화면을 파악하고 영향받는 화면을 검증한다.
- 기존 기능이 깨지거나 데이터 의미가 달라지지 않았는지 확인한다.

### 자동 검사

- 프로젝트에서 정한 타입 검사·린트·테스트·빌드 등 필수 검사를 실행한다.
- 변경한 로직과 주요 실패 조건을 검증하는 테스트를 추가하거나 갱신한다.
- 테스트를 통과시키기 위해 기대값만 바꾸거나 검사를 제거하지 않는다.
- 자동 검사 통과만으로 실제 동작·화면 검증을 대신하지 않는다.

### 실제 동작 검증

- 브라우저에서 구현한 기능을 직접 조작한다.
- 진입부터 완료까지 사용자의 실제 이용 순서대로 확인한다.
- 버튼·링크·입력·선택·모달·뒤로 가기·새로고침 등
  해당 기능에 필요한 조작을 확인한다.
- 중복 클릭·반복 실행·빠른 화면 전환 등 관련 예외 상황을 확인한다.
- 저장 기능은 테스트 환경에서 저장 후 다시 조회해 결과까지 검증한다.
- 콘솔 오류와 실패한 네트워크 요청도 확인한다.

### 디자인·레이아웃 검증

- 실제 화면을 사용자가 선택한 시안 및 디자인 가이드와 비교한다.
- 색상·글꼴·굵기·간격·정렬·크기·모서리·아이콘·공용 부품의
  규격이 가이드와 일치하는지 확인한다.
- 눈대중만으로 판단하지 않는다.
  필요한 경우 스크린샷·요소 크기·계산된 스타일을 확인한다.
- 가이드에서 정한 화면 폭과 레이아웃 전환 경계의 전후를 확인한다.
- 긴 텍스트·많은 항목·데이터 없음에서도
  잘림·겹침·의도하지 않은 가로 스크롤이 없는지 확인한다.
- 키보드 탐색·포커스·폼 라벨·터치 조작 등 관련 접근성을 확인한다.
- 로딩 중 화면 이동·중복 표시·레이아웃 흔들림도 확인한다.

### 수정과 재검증

- 발견한 문제를 수정하고 해당 항목과 영향 범위를 다시 검증한다.
- 기존 문제와 이번 변경으로 발생한 문제를 구분한다.
- 환경·계정·데이터가 부족하면 가능한 검증부터 진행하고,
  미확인 항목과 필요한 조건을 명시한다.
- 검증하지 않은 항목을 통과했다고 쓰거나
  남은 문제가 있는데 ‘전체 검증 완료’라고 보고하지 않는다.

## 문서와 완료 보고

- 기능이나 디자인 규칙을 변경하면 관련 문서와 테스트도 함께 맞춘다.
- 결정 기록은 이유를 포함해 간결하게 남긴다.
- 최종 보고에는 다음을 포함한다.
  - 무엇을 왜 변경했는지
  - 검증한 화면·상태·환경과 자동 검사 결과
  - 미실행 검사·미확인 사항·남은 문제
  - 변경 파일과 복사 가능한 Git 명령·커밋 제목
- 시각적 변경은 결과를 확인할 수 있는 스크린샷이나 아티팩트를 남긴다.
- 실제로 수행한 작업과 확인한 사실만 보고한다.